import { PublicKey, TransactionInstruction } from '@solana/web3.js';
import { AccountInfoMap, Amm, Quote, QuoteParams, SwapParams } from '../amm';
import { SerumMarket } from '../market';
export declare class SerumAmm implements Amm {
    market: SerumMarket;
    label: "Serum";
    private orderbooks;
    constructor(market: SerumMarket);
    get id(): string;
    getAccountsForUpdate(): PublicKey[];
    update(accountInfoMap: AccountInfoMap): void;
    getQuote({ sourceMint, destinationMint, amount }: QuoteParams): Quote;
    createSwapInstructions({ sourceMint, sourceTokenAccount, destinationTokenAccount, userTransferAuthority, amount, minimumOutAmount, openOrdersAddress, platformFee, quoteMintToReferrer, }: SwapParams): TransactionInstruction[];
    get reserveTokenMints(): PublicKey[];
}
