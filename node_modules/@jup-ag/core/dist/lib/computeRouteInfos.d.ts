/// <reference types="node" />
import { AccountInfo, Connection, PublicKey } from '@solana/web3.js';
import { Route, RouteInfo, TransactionFeeInfo } from './routes';
export declare function fetchRoutes(connection: Connection, routes: Route[], extraAccountInfosMap?: Map<string, AccountInfo<Buffer>>): Promise<Route[]>;
interface GetQuotesParams {
    routes: Route[];
    amount: number;
    inputMint: PublicKey;
    outputMint: PublicKey;
    platformFeeBps: number;
    slippage: number;
    getDepositAndFeeForRoute: (marketInfos: RouteInfo['marketInfos']) => Promise<TransactionFeeInfo | undefined>;
}
export declare const computeRouteInfos: ({ routes, amount, inputMint, outputMint, platformFeeBps, slippage, getDepositAndFeeForRoute, }: GetQuotesParams) => RouteInfo[];
export {};
