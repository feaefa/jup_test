import JSBI from 'jsbi';
export declare class Curve {
    private numberOfCurrencies;
    private amplificationFactor;
    private tokenAmounts;
    private targetPrices;
    constructor(numberOfCurrencies: JSBI, amplificationFactor: JSBI, tokenAmounts: JSBI[], targetPrices: JSBI[]);
    exchange(inputIndex: number, outputIndex: number, amount: JSBI, minusOne?: boolean): JSBI;
    computeBaseY(inputIndex: number, outputIndex: number, amount: JSBI): JSBI;
    private computeY;
    private computeD;
    private xp;
}
