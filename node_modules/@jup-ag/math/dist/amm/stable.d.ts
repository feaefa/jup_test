import JSBI from 'jsbi';
import { SwapResult } from '../utils';
import { Fraction } from '..';
export declare class Stable {
    private tokenAmounts;
    private targetPrices;
    private traderFee;
    private curve;
    constructor(amp: JSBI, tokenAmounts: JSBI[], targetPrices: JSBI[], traderFee: Fraction);
    exchange(inputTradeAmount: JSBI, slippageTolerance: Fraction, inputIndex: number, outputIndex: number): SwapResult;
    private getPriceImpact;
    private getFees;
    private getMinimumOutputAmount;
    private getOutputAmount;
    private getOutputAmountWithNoSlippage;
}
